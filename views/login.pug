doctype html
html(lang="en")
    include includes/head.pug
    body(class="flex flex-col min-h-screen bg-gray-100")
        main(class="flex-grow container mx-auto p-4 items-center justify-center")
            div(class="flex flex-col md:flex-row h-full items-center items-center justify-center py-0 md:py-4 lg:py-8")
                div(class="py-8 md:py-0 text-center md:text-left")
                    h1(class="font-bold text-4xl md:text-5xl max-w-xl text-gray-900 leading-tight")
                        | Aplicación para subir imágenes a Google Drive
                    span(class="w-12 h-1 bg-orange-500 rounded-full mt-8")
                    p(class="text-gray-800 text-base leading-relaxed mt-8 font-semibold")
                        | Sube tus imágenes a tu cuenta de Google Drive de forma sencilla.
                    div(class="get-app flex space-x-5 mt-10 justify-center md:justify-start")
                        a(
                            class="bg-red-600 hover:bg-red-500 text-red-100 py-2 px-4 rounded inline-flex items-center"
                            href="/auth/google"
                        )
                            p(class="font-semibold")
                                | Iniciar sesión con Google
                div(class="max-w-xl")
                    img(src="/img/image-upload.svg" alt="Image Upload")
        include includes/footer.pug