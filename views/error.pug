doctype html
html(lang="en")
    include includes/head.pug
    body
        div(class="bg-gray-100")
            div(class="container mx-auto")
                div(class="grid grid-cols-1 items-center p-2")
                    h1(class="font-bold text-4xl md:text-5xl text-gray-900 text-center")
                        | Ha ocurrido un error.
                    div(class="get-app flex space-x-5 my-4 justify-center")
                        a(
                            class="bg-gray-600 hover:bg-gray-500 text-gray-100 py-2 px-4 rounded inline-flex items-center"
                            href="/"
                        )
                            p(class="font-thin")
                                | Regresar
        include includes/footer.pug